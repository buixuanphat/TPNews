<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TPNewsỨngDụngĐọcBáo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/tab_selected_color</item> <!-- Màu nền sáng -->
        <item name="colorPrimaryDark">@color/tab_selected_color</item>
        <item name="colorAccent">@color/tab_selected_color</item>
    </style>

    <style name="Theme.TPNewsỨngDụngĐọcBáo" parent="Base.Theme.TPNewsỨngDụngĐọcBáo" />
</resources>